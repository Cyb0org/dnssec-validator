{
  "name": "DNSSECValidatorPlugin",
  "version": "${FBSTRING_PLUGIN_VERSION}",
  "description": "Check DNSSEC status",

	"plugins": [
    { "path": "npDNSSECValidatorPlugin.dll", "public": true }
  ]
}